spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/geometry
    username: postgres
    password: amiri
    driverClassName: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: validate