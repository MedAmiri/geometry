package org.geometry.user.repository;

import org.geometry.user.entities.Loging;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogingRepository extends JpaRepository<Loging, Long>{
}
